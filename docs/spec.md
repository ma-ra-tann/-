目的（Why）

本ドキュメントは、既に保有している「Seed投資を行うVCリスト（会社名・ステージ付き）」を起点として、財務モデルに明確な興味・知見を持つ投資家（キャピタリスト）だけを特定・管理する仕組みを定義することを目的とする。

本ツールは新たにVCを網羅的に探すものではなく、手元にあるSeed VCリストをいかに精度高く“使える状態”に変換するかにフォーカスする。

⸻

前提条件（Assumptions）
	•	Seed投資を行うVCの一覧は既に存在する
	•	VCリストには以下の情報が含まれている
	•	VC会社名
	•	投資ステージ（Seed / Pre-Seed など）
	•	本ツールでは VCの有無・適格性は再評価しない
	•	評価対象は VCに所属するキャピタリスト個人のみ

⸻

絶対条件（非交渉）
	•	必須条件は 「財務モデルそのものに興味・知見があること」
	•	経営管理・BI・ダッシュボード止まりの人物は対象外
	•	財務モデルへの言及・投資実績・判断軸が確認できない場合は 自動除外

⸻

ゴール定義（What）

以下が即答できる状態を作る。
	1.	このVCの中で、誰に当てるべきか？
	2.	なぜその人なのか？（証拠ベースで）
	3.	この人には何を刺しに行くべきか？
	4.	逆に、誰には当たらないか？

⸻

全体像（How this works）

本ツールは、既存VCリストを入力として、以下の処理を行う。
	1.	VC会社情報の補完取得
	2.	VC配下キャピタリストの抽出
	3.	キャピタリストごとの財務モデル関連性分析
	4.	スコアリングと自動判定
	5.	検索・閲覧可能なUIでの可視化

⸻

入力データ（Input）

既存VCリスト（前提データ）

最低限以下のカラムを持つ。
	•	VC会社名（Primary Key）
	•	投資ステージ

※ 本ツールはこのリストに記載されたVCのみを対象に処理を行う

⸻

データ取得方針（VCはWeb検索で補完）
	•	VCの追加探索はしない（リスト外は扱わない）
	•	リストに記載された各VCをWeb検索し、VC会社情報とキャピタリスト情報を補完取得する
	•	取得元の優先順位（信頼性順）：
	1.	VC公式サイト（Team / People / Portfolio / Thesis など）
	2.	公式発表・公式ブログ
	3.	信頼性の高い第三者DB（例：Crunchbase等）
	4.	インタビュー記事・イベント登壇ページ

⸻

機能一覧（MVPスコープ）

1. VC検索機能（既存VCリスト起点）

Input
	•	VC会社名（部分一致）

Output

VC会社情報（補完取得）
	•	VC名
	•	WebサイトURL
	•	投資ステージ（既存データ）
	•	投資テーマ
	•	代表的投資実績（3〜5社）
	•	投資方針サマリ（AI要約・1〜2行）

キャピタリスト一覧（判定順）
	•	氏名
	•	役職
	•	投資担当領域
	•	判定（⚪︎ / △ / ✖︎）
	•	判定理由（文章・1行）

⸻

2. キャピタリスト詳細表示（VC検索結果起点）

Input
	•	VC検索機能（1）のOutputに含まれるキャピタリスト名

※ キャピタリスト名を直接入力する検索機能は持たない
※ 必ず「VC → キャピタリスト」の導線を通す

⸻

Output
	•	氏名 / 役職 / 所属VC
	•	投資担当領域
	•	財務モデルとの関係性（定性的評価）
	•	判定（GO / 補欠 / 除外）
	•	判定理由（文章・2〜3行）
	•	財務モデルとの具体的接点
	•	投資実績
	•	発言・記事・登壇内容（要約）

👉 「この人と話すべきか」を文章で判断できる状態を作る

⸻

3. 財務モデル特化フィルター
	•	財務モデリング / FP&A 投資実績あり
	•	財務モデル言及あり（記事・SNS・登壇）
	•	Financeバックグラウンドあり

※ 経営管理・BI止まりは除外

⸻

4. 定性的判定 & 自動分類

設計思想
	•	数値スコアは使用しない
	•	定性的な証拠（文章・文脈）をもとに分類する
	•	判定は必ず以下の3分類のいずれかに落とす

⸻

判定区分（表示用ステータス）

判定は数値を使わず、以下の3段階の記号ステータスで表現する。
	•	⚪︎（GO）：
	•	財務モデルへの強い興味・知見が明確
	•	初回面談から財務モデルの議論が成立する
	•	△（補欠）：
	•	財務モデルへの一定の理解は見られる
	•	ただし投資判断軸の中心ではない
	•	✖︎（除外）：
	•	財務モデルへの言及・関心が確認できない
	•	経営管理・BI・マーケ中心

⸻

判定方法（例）
	•	投資理由・発言内容の文脈解析
	•	過去投資先の性質（モデル中心か否か）
	•	記事・SNS・登壇での語り口

※ 「数値化できないため排除」ではなく、
数値化せずに判断できるレベルの根拠を集める

⸻

5. 除外理由の明示（意思決定ログ）
	•	除外理由を必ず保持
	•	財務モデル言及なし
	•	投資判断軸がマーケ中心
	•	経営管理ツール止まり

⸻

6. アプローチ補助（最低限）
	•	このキャピタリスト向けの刺しどころ（仮説・1〜2行）
	•	財務モデル文脈での会話起点

※ メール文生成・CRM連携などはMVP外

⸻

技術前提（Implementation Notes）
	•	Claude Code / Cursor を前提
	•	入力VCリスト → Web検索で補完 → キャピタリスト抽出 → 定性判定 → 表示
	•	データは JSON / SQLite
	•	UIは検索・一覧・詳細のみの軽量Web

※ 一覧表示時は ⚪︎ → △ → ✖︎ の順で自動並び替えを行う

⸻

LLMエージェンティック実行方針（Agentic Design）

本フローはLLMを「要約係」ではなく、**複数ステップの実行主体（エージェント）**として用い、VCごとに反復処理する。

役割分担（最小構成）

1) Orchestrator（司令塔）
	•	入力：既存VCリスト（会社名・ステージ）
	•	役割：VCごとに下記エージェントを順に実行し、結果を統合・保存
	•	出力：UI表示用データ（VCページ + キャピタリスト一覧 + 詳細）

2) VC Research Agent（VC会社情報の補完）
	•	Web検索し、VC会社情報を補完取得
	•	取得：公式サイトURL / 投資テーマ / 代表投資実績 / 投資方針サマリ
	•	重要：情報ソースURLを必ず保持

3) Capitalist Extractor Agent（キャピタリスト抽出）
	•	公式サイトの Team / People から投資担当者を抽出
	•	取得：氏名 / 役職 / プロフィールURL（可能なら）
	•	重要：キャピタリスト詳細は「VC検索結果から選択」導線のみ

4) Evidence Collector Agent（根拠収集）
	•	各キャピタリストについて、財務モデル関連の根拠をWeb検索で収集
	•	対象：投資実績、インタビュー、寄稿、登壇、SNS言及（可能な範囲）
	•	出力：根拠の要約 + 参照URL

5) Qualitative Judge Agent（定性判定）
	•	収集した根拠を読み、⚪︎ / △ / ✖︎ を決定
	•	出力：
	•	判定（⚪︎/△/✖︎）
	•	判定理由（2〜3行）
	•	会話の刺しどころ（1〜2行）
	•	制約：数値スコアは禁止

実行の基本ルール
	•	すべての主要アウトプットに「根拠URL」をひも付ける
	•	根拠が弱い場合は、判定を上げない（⚪︎にしない）
	•	不確実な場合は △ に寄せ、理由に不確実性を明示

データ保存（推奨）
	•	raw_sources：取得したURLと抜粋/要約
	•	vc_profile：VC会社情報
	•	capitalists：VC配下キャピタリスト一覧
	•	capitalist_detail：個人詳細（判定・理由・根拠）

⸻

このツールで実現したい本質
	•	既存VCリストの価値を最大化する
	•	説明コストがかかる相手を事前に排除する
	•	Seed調達の打率を構造的に上げる

⸻

最終アウトカム
	•	VCはもう探さない
	•	誰に当てるかで迷わない
	•	無駄な面談がなくなる

以上